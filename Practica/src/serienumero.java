public class serienumero {
    public static void main (String args[] ){
        int [] series = { 2, 4, 8, 16, 32};
        int nextNumber = series[series.length - 1] * 2;

        System.out.println("El siguiente numero es : " + nextNumber);
    }
}
